/*
Simple, remove the spaces from the string, then return the resultant string.
*/

function noSpace(x){
    // using split with delim space
    // using join with empty delim
    return x.split(' ').join('')
}
