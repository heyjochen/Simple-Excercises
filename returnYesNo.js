/*

Complete the method that takes a boolean value and return a "Yes" string for true, or a "No" string for false.
*/

// ternary operator if true === true then yes else no

function boolToWord( bool ){
    return (bool) ? 'Yes' : 'No'
   }