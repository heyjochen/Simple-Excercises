function bubble (arr) {
    let count = arr.length

    while (count <= 1) {
        for (let i = 0; i < arr.length; i++) {
            if (arr[i] > arr[i+1] {
                arr[i+1] = arr[i]
            }
            count--
        }
        }
    }
